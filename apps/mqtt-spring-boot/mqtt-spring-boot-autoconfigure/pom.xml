<?xml version="1.0" encoding="UTF-8"?>
<!--
  The code and the @AutoConfiguration classes.

  Every third-party dependency here is <optional>true</optional>. Optional means
  "needed to compile, but not forced on anyone who depends on me" – so a consumer
  of this module alone brings its own Paho, Jackson and Micrometer versions, and
  is never silently upgraded or downgraded by ours. The sibling starter is what
  supplies the opinionated set for consumers who want the decisions made for them.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.jinternals.mqtt</groupId>
    <artifactId>mqtt-spring-boot</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>mqtt-spring-boot-autoconfigure</artifactId>
  <name>mqtt-spring-boot-autoconfigure</name>
  <description>Auto-configuration for a durable MQTT client</description>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>

    <dependency>
      <groupId>org.eclipse.paho</groupId>
      <artifactId>org.eclipse.paho.mqttv5.client</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.datatype</groupId>
      <artifactId>jackson-datatype-jsr310</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-actuator</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-actuator-autoconfigure</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-configuration-processor</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <!--
            The processor is also declared as an optional dependency above, which
            is how Spring Boot documents it and is enough on JDK 21. Naming it
            here as well makes annotation processing EXPLICIT rather than relying
            on javac discovering processors on the compile classpath – behaviour
            that is deprecated from JDK 21 and off by default from JDK 23, at
            which point the metadata would silently stop being generated and the
            only symptom would be IDE completion quietly disappearing.
          -->
          <annotationProcessorPaths>
            <path>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-configuration-processor</artifactId>
              <version>${spring-boot.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
